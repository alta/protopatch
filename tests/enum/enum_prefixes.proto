syntax = "proto3";

// clang-format off
package tests.enum;
// clang-format on

import "patch/go.proto";

option go_package = "github.com/alta/protopatch/tests/enum";

enum EmptyPrefixes {
	ZERO = 0 [(go.value).prefix = ""];
	ALPHA = 1 [(go.value).prefix = ""];
	BETA = 2 [(go.value).prefix = ""];
}

enum CustomPrefixes {
	NONE = 0 [(go.value).prefix = "Custom"];
	RED = 1 [(go.value).prefix = "Custom"];
	BLUE = 2 [(go.value).prefix = "Custom"];
}
